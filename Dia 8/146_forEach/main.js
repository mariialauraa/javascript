const numeros = [1, 2, 3, 4, 5, 6]

numeros.forEach(num => {
    console.log(num)
})

const nomes = ["Maria", "Jose", "Joao", "Joana"]

nomes.forEach(nome => {
    console.log("O nome é " + nome)
})

// numeros[i] e nomes[i]